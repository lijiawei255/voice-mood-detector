# ==============================================================================
# 语音情绪识别系统 (Voice Mood Detector) - Python 依赖清单
# ==============================================================================
# 注意：建议使用 Python 3.8~3.11，推荐使用 Anaconda 创建独立环境
# 如果 PyAudio 安装失败，请使用: conda install -c conda-forge pyaudio -y
#
# 说明：
# - 仅列出本项目代码直接 import 的依赖；它们的间接依赖会由 pip 自动安装。
# - funasr / modelscope 加上界以保证科研可复现性（API 可能有破坏性变更）。
# - 测试依赖见 requirements-dev.txt
# ==============================================================================

# ----- GUI 界面 -----
PyQt5>=5.15.0

# ----- 音频录制与处理 -----
pyaudio>=0.2.11
librosa>=0.9.0
soundfile>=0.12.0
praat-parselmouth>=0.4.0

# ----- AI 模型与推理 -----
modelscope>=1.9.0,<2.0
funasr>=1.0.0,<2.0
torch>=1.13.0

# ----- 数据处理与可视化 -----
matplotlib>=3.5.0
numpy>=1.21.0
scipy>=1.7.0
